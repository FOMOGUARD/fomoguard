# 투자워워 (구 포모가드) — 인수인계 메모

마지막 업데이트: 2026-08-28
새 컴퓨터/새 Claude Code 세션에서 이 프로젝트를 이어가려면 이 파일부터 읽으세요.

---

## 1. 프로젝트가 뭔가

- **무엇**: 개인 투자자를 위한 "투자 원칙 지킴이" 웹앱. 충동 매수를 AI가 냉철하게 검증해주는 포모체크, 포트폴리오/현금 관리, 실시간 뉴스, 투자 격언, RPG식 성장(레벨/XP) 등을 제공.
- **형태**: 프레임워크 없는 순수 HTML/JS 단일 파일(`index.html`, ~200KB+, 3500줄+) + Cloudflare Pages Functions(서버 프록시) 여럿. 빌드 스텝 없음.
- **타겟**: 투자를 무겁게 느끼는 2030 세대. 톤앤매너는 "쉽고 편하게" + 냉철한 팩트 + 투자 커뮤니티 밈 유머.
- **서비스명**: 아직 확정 안 됨. 현재 코드 전체에 **"투자워워"(가칭)**로 반영되어 있음. 후보: 투자워워(1순위 추천), 포모레이더, 포모스캐너. 결정되면 `grep -n "투자워워" index.html`로 전체 치환하면 됨(전부 화면 텍스트, 로직에 영향 없음).

## 2. 저장소 / 배포

- **GitHub**: https://github.com/FOMOGUARD/fomoguard (브랜치: `main`)
- **현재 운영 도메인**: `https://fomoguard.pages.dev` (Cloudflare Pages, GitHub 연동 → `main` push할 때마다 자동 배포, 보통 15~30초 소요)
- **레거시**: `https://fomoguard.netlify.app` — Netlify는 빌드 크레딧 소진으로 더 이상 신규 배포 안 됨. 롤백 대비용으로 `netlify.toml` / `netlify/functions/`는 코드에 남겨둠 (Cloudflare Functions와 거의 동일한 로직을 이 폴더에도 미러링해서 유지 중. `functions/`와 `netlify/functions/`는 항상 쌍으로 수정할 것).
- **Cloudflare Pages 빌드 설정**: Build output directory = `/` (루트). Functions는 `functions/*.js` 파일 기반 라우팅(파일명 = 경로, 예: `functions/ai-free.js` → `/ai-free`).
- **Cloudflare 환경변수** (대시보드 → 프로젝트 → Settings → Environment variables에 등록되어 있어야 함, Production+Preview 둘 다):
  - `GEMINI_SHARED_KEY` — 무료 AI 티어용 공유 Gemini API 키. **⚠️ 실측 결과 무료 티어 하루 요청 한도가 20건으로 매우 낮음**(모델: `gemini-3.6-flash`, `/ai-free`와 `/news-free`가 이 한도를 같이 나눠 씀). 소량 테스트만으로도 당일 한도가 소진돼 429가 남 — 아래 4번 섹션의 뉴스 진단 기록 참고. **실사용 트래픽을 감당하려면 이 Google AI Studio 프로젝트의 결제(유료 티어)를 활성화하는 게 사실상 필수**로 보임(사용자 판단 필요, 비용 발생).
  - `SUPABASE_URL`, `SUPABASE_ANON_KEY` — `/ai-free`에서 사용자 로그인 세션 검증용 (index.html에도 동일 값이 하드코딩되어 있음, anon key라 공개되어도 무방)
  - `NEWSDATA_SHARED_KEY`, `CURRENTS_SHARED_KEY` — 등록 완료·배포 서버에서 정상 작동 확인됨(2026-08-24). `/news-free`(GNews 키 없는 사용자용 기본 뉴스 경로)가 사용.
  - `SUPABASE_SERVICE_ROLE_KEY` — **⚠️ 아직 미등록.** `/delete-account`(회원탈퇴)가 사용하는 관리자 권한 키. Supabase 대시보드 → Settings → API → "service_role secret" 복사해서 등록 필요. **주의: 이 키는 RLS를 완전히 우회하는 최고 권한 키라 클라이언트 코드(index.html)에는 절대 넣으면 안 되고, Cloudflare 환경변수로만 등록할 것.** 없으면 회원탈퇴 버튼을 눌러도 "서버에 회원탈퇴 설정이 아직 안 되어 있습니다" 에러만 반환하고 계정은 안전하게 유지됨(앱 자체는 안 깨짐).
  - `/price-free`(시세, Yahoo Finance 기반)는 키가 필요 없음 — 별도 환경변수 등록 불필요.

## 3. 백엔드 / 데이터

- **Supabase** 프로젝트 하나로 인증 + DB 겸용. `index.html` 상단에 `SUPABASE_URL`/`SUPABASE_ANON_KEY` 하드코딩되어 있음(공개 anon key, 문제 없음).
- **로그인**: Google OAuth (Supabase Auth). 로그인 안 해도 전 기능 브라우저 로컬에서 사용 가능(비로그인 "둘러보기"는 예시 데이터로 자동 채워짐 — 아래 4번 참고).
- **클라우드 동기화 테이블**: `fomoguard_data` (컬럼: `user_id`, `data` jsonb, `updated_at`). RLS로 `auth.uid() = user_id`만 접근 가능 — 실제 익명 접근 테스트로 검증 완료(다른 유저 데이터 절대 안 보임).
  - 동기화 대상(`SYNCABLE_KEYS`): principles, portfolio, journal, transactions, factchecks, snapshots, newsKeywords, cashFlows, settings(API 키 포함). **growth(레벨/XP)는 여기 포함 안 됨** — 아래 성장 테이블에서 서버 검증으로 별도 관리.
  - 로그인 상태에서 다른 기기의 변경사항을 반영하기 위해 **탭 포커스/visibility 복귀 시 자동으로 클라우드를 다시 pull**함(로컬에 아직 push 안 된 변경이 있으면 스킵, 너무 잦은 재호출은 스로틀).
- **AI 사용량 제한 테이블**: `fomoguard_ai_usage` + RPC `increment_ai_usage(p_cap)` — 하루 3회 무료 AI 사용량 카운트. SQL은 `supabase-ai-usage.sql` 참고. (이미 실행 완료된 것으로 보임)
- **성장(레벨/XP) 테이블**: `fomoguard_growth` + RPC 4종(`grant_login_xp`, `grant_factcheck_xp`, `grant_journal_xp`, `submit_quiz_xp`). SQL은 `supabase-growth.sql`. **실행 완료 확인됨(2026-08-24)** — 익명 키로 RPC를 직접 호출해서 `fomoguard_growth` 테이블·함수가 실제로 존재하는 것을 검증함(비로그인 호출은 `user_id` NOT NULL 제약으로 정상 거부되는 것까지 확인, 로그인 사용자는 정상적으로 XP가 쌓임). 예전엔 클라이언트가 XP를 직접 계산해서 클라우드에 올렸는데, devtools로 조작 가능한 취약점이라 서버 검증(RLS: SELECT만 허용, INSERT/UPDATE는 전부 security-definer RPC를 통해서만)으로 바꾼 것.
- **AI 프로바이더**: 기본은 Claude/Gemini/OpenAI/Grok 중 사용자가 본인 키 등록(무제한). 로그인만 한 사용자는 `/ai-free` 프록시를 통해 하루 3회 무료(Gemini, 서버 공유 키 사용, 클라이언트에 키 노출 안 됨).

## 4. 작업 이력 (세션별, 최신순)

### 세션 5 — 2026-08-27, QA 감사 및 개선

전체 서비스를 보안·UX 관점에서 감사하고 우선순위대로 수정했다. 감사 리포트(아티팩트)에
근거와 재현 방법을 정리해뒀다.

**보안 — 실제로 공격해보고 확인한 것들**

1. **[치명적, 수정완료] `/news-free`·`/price-free`가 인증 없이 완전 공개돼 있었음.**
   외부에서 curl로 호출하면 HTTP 200으로 응답하며 **전체 사용자 공유 키(NewsData·Currents·Gemini)를 소모**했다.
   Gemini 무료 쿼터가 하루 20건뿐이라 스크립트 한 번이면 그날 전 사용자의 뉴스 번역이 죽는 구조였고,
   그동안 반복적으로 겪은 "번역이 안 된다" 증상의 유력한 경로이기도 하다.
   - `/news-free`: `/ai-free`와 동일하게 Supabase 토큰 검증 추가(로그인 필수). 공유 키를 쓰는 경로라 엄격하게 잠금.
     본인 GNews 키 등록자는 `/news-proxy`를 타므로 영향 없음. 클라이언트도 토큰을 함께 전송하도록 수정.
   - `/price-free`: 야후 무료 시세라 비용이 없어 로그인까지 요구하면 둘러보기 사용자만 불편해진다.
     대신 **Origin/Referer 출처 검사**로 외부 스크립트·크롤러만 차단(브라우저는 교차 출처 Origin을 위조할 수 없음).
   - 검증: 무인증 curl → news-free 401 / price-free 403, 위조 Origin → 403, 정상 Origin → 200,
     실제 앱에서 지수·시세 정상 로드 확인.
2. **[수정완료] 보안 헤더 부재** — `_headers`에 캐시 설정만 있었다.
   `frame-ancestors 'none'` + `X-Frame-Options: DENY` 추가로 클릭재킹 차단
   (회원탈퇴·매도 기록처럼 되돌릴 수 없는 버튼이 있어 금융 앱엔 필수).
   Permissions-Policy도 추가. **전체 CSP는 아직 미적용** — Tailwind CDN이 런타임 컴파일에 `unsafe-eval`을
   요구해서 허용 목록을 정리해야 하며, 앱을 깨뜨리지 않으려면 별도 작업이 필요하다(남은 과제).
3. **[문서화, 부분완료] 사용자 API 키가 평문 저장·동기화됨** — localStorage와 Supabase에 평문 JSON.
   RLS 덕에 타인은 못 읽지만 DB 접근 권한자·유출 시엔 전원 키가 노출되고, 과금되는 키라 금전 피해로 직결된다.
   구조 변경(서버 보관)은 크므로 이번엔 **설정 화면에 키 보관 위치를 정직하게 명시**하는 것으로 처리.
   근본 해결은 남은 과제.
4. **[수정완료] 메인 테이블 스키마가 저장소에 없었음** — 모든 사용자 데이터가 들어가는
   `fomoguard_data`의 정의·RLS 정책이 버전 관리에 빠져 있었다. `supabase-data.sql`로 커밋.
   (growth/ai-usage SQL만 있었음)

**보안 — 공격 시도했으나 막힌 것(정상 동작 확인)**
- RLS: 익명 키로 전체 조회 시 빈 배열, 타인 행 삽입 시 `42501`로 거부.
- XSS: 뉴스 제목·AI 응답·일지·원칙 전부 `escapeHtml` 경유, 링크는 `safeUrl`로 http(s)만 허용.
- 회원탈퇴: 클라이언트가 보낸 id를 믿지 않고 토큰으로 본인 재확인 후 삭제. service_role은 서버 전용.
- 경험치: 테이블에 INSERT/UPDATE 정책 없이 security definer RPC로만 지급.

**UX — "쉽고 편하게" 관점 개선**

5. **[치명적, 수정완료] 포모체크가 자기 안내문을 못 지켰음.**
   입력창에 "예: 테슬라 또는 TSLA"라고 써있는데 시세 API는 티커만 받아서, **안내문대로 "테슬라"를 치면 실패**했다.
   "삼성전자"도 실패하고 "005930"만 성공. 게다가 시장 기본값이 "자동(해외)"라 한국 초보의 첫 시도는 거의 무조건 실패.
   → **`STOCK_NAME_MAP` 한글 종목명 사전 추가**(국내 주요종목·ETF + 해외 인기종목 100여 종).
   이름을 치면 종목코드·시장이 자동으로 잡히고 미리보기에 "삼성전자 (005930) · 현재가 …"로 표시된다.
   시장 기본값도 국내(KRX)로 변경. 사전에 없으면 입력값을 코드로 간주(기존 동작 유지).
   `fetchLivePrice` 자체에 넣어서 포트폴리오 시세 갱신도 함께 혜택을 본다.
6. **[수정완료] 토스트가 헤더 로고를 덮어씀** — `#toast-wrap`이 `top-4` 고정이라 헤더와 물리적으로 겹쳐
   신규 사용자 첫 화면에서 로고 글자가 뭉개졌다. 헤더 아래(`top:5.25rem`)로 이동.
7. **[수정완료] 로그인 전 "+10 EXP" 토스트** — EXP가 뭔지 모르는 시점의 축하는 노이즈라,
   온보딩 완료 전에는 토스트를 띄우지 않음(적립은 그대로).
8. **[수정완료] 예시 데이터 표식 부재** — 둘러보기 대시보드가 가짜 자산 878만원을 표식 없이 크게 보여줬다
   (토스트는 사라짐). 총자산 카드에 "👀 예시 데이터 · 내 돈이 아니라 체험용 숫자예요" 배지 상시 노출.
9. **[수정완료] 비로그인 사용자가 실패할 버튼을 누르게 둠** — 버튼 라벨을 "로그인하고 검증받기"로 바꿔
   결과를 미리 알리고, 누르면 탭을 강제로 옮기던 것을 **제자리 로그인 유도 카드**로 변경(입력 내용 보존).
10. **[수정완료] 설정이 7섹션·2.1화면, 그중 3개가 API 키** — AI·시세·뉴스 연동을
    `<details>` "🔧 고급 설정 · API 키 연동 (선택)"으로 접었다. **2.1화면 → 1.1화면**으로 축소.
    접힌 상태에서도 저장이 정상 동작하는 것 확인.
11. **[수정완료] 중복 제거** — 설정 상단 로고(헤더에 이미 있음) 제거,
    탭 순서 편집 진입점을 더보기 한 곳으로 일원화(설정에서 제거).
    ※ 리포트에선 "설정으로 일원화"라고 적었으나, 더보기가 탭 목록을 보여주는 화면이라
    그 옆에서 순서를 바꾸는 게 맥락상 자연스러워 반대로 결정했다.

**검토했으나 버그가 아니었던 것**
- 자산가치 추이 그래프: `snapshotToday()`가 매일 `computePortfolioStats()`의 `totalValue`(현금 제외
  보유종목 평가금액 합)를 기록하고 최근 30일을 그린다. 툴팁에도 "포트폴리오 평가금액"이라 명시돼 있어
  설계대로 동작하는 것. 다만 **현금을 제외**하므로 입출금은 그래프에 반영되지 않는다(의도된 동작).

**추가 수정 (같은 세션, 후속 작업)**

12. **[수정완료] 대시보드 카드 편집 진입점 노출** — 2초 롱프레스로만 진입 가능해 초보는 존재를 몰랐다.
    "🧩 화면에 보일 카드 고르기" 버튼을 대시보드 하단에 추가(롱프레스도 그대로 유지).
    대시보드 밀도 문제를 사용자가 직접 해결할 수 있게 하는 것이 기본값을 임의로 줄이는 것보다 낫다고 판단.
13. **[내가 만든 버그, 수정완료] 티커 축약 매핑 충돌** — `STOCK_NAME_MAP`에 `'ms':'MSFT'`를 넣었는데
    **MS는 실제로 모건스탠리 티커**($214)라, 모건스탠리를 조회하려던 사용자에게 마이크로소프트 시세·분석이
    조용히 나올 수 있었다. 제거함. **돈이 걸린 화면에서 "조용히 틀린 결과"는 매핑이 없는 것보다 나쁘다** —
    앞으로 사전에 축약어를 추가할 땐 실제 티커와 충돌하는지 반드시 확인할 것.
    (`kt`는 국내 030200과 NYSE KT가 같은 회사라 무해해서 유지)
14. **[내가 만든 버그, 수정완료] 예시데이터 배지 조건 오류** — 조건을 `!state.profile`(비로그인 전체)로 걸어서,
    **비로그인 상태로 본인의 실제 데이터를 입력한 사용자에게도 "내 돈이 아니라 체험용 숫자예요"라고 표시**됐다.
    데이터가 없어 0원인 화면에도 떴다. 이미 존재하던 `fomoguard_demo_seed` 플래그를 쓰도록 수정
    (실제 데모 시드가 있을 때만 표시). 두 시나리오 모두 브라우저에서 검증함.

**검증 방법 메모 (다음에도 유용)**
- 엔드포인트 보호 확인: 무인증 curl / 위조 Origin / 정상 Origin 세 가지를 각각 호출해
  401·403·200이 나오는지 본다. "막혔다"만 확인하지 말고 **정상 요청이 여전히 통과하는지**를 꼭 같이 본다.
- 토큰 검증이 진짜인지 확인: 위조 Bearer 토큰으로 호출해 401이 나오는지 본다.
  헤더 존재만 체크하는 코드라면 200이 나온다.
- 화면 검증 시 `fade-in` 애니메이션 때문에 렌더 직후 스크린샷이 빈 화면으로 찍힐 수 있다.
  빈 화면이 보이면 콘솔 에러와 `panel.innerHTML.length`를 먼저 확인할 것(실제 버그와 구분).

**CSP 전체 적용 (완료)**

15. **[완료] Content-Security-Policy 강제 적용.** `_headers`에 전체 정책 배포.
    - **적용 절차(다음에도 이 방식 권장)**: ①코드에서 브라우저가 부르는 외부 도메인 전수 조사 →
      ②**Report-Only로 먼저 배포**(아무것도 차단 안 됨) → ③`securitypolicyviolation` 리스너로 위반 수집 →
      ④위반 0건 확인 후 강제 전환 → ⑤전체 회귀 테스트.
    - **탐지 방법이 맞는지부터 검증**했다: 일부러 허용목록 밖 이미지를 로드해 위반이 잡히는 걸 확인한 뒤
      "위반 0건"을 신뢰했다. (탐지가 고장난 채 0건을 보고 안심하는 실수를 피하려고)
    - 검증 범위: 첫 진입·온보딩, 환율/시세/지수/뉴스 새로고침(실제 값 갱신 확인),
      8개 탭 전체 순회, 현금 모달, 포트폴리오 시세 갱신, 차트 3종 렌더링,
      data:/blob:/구글프로필 이미지 경로. 강제 전환 후에도 위반 0건·전 기능 정상.
    - **한계를 정확히 알 것**: 인라인 스크립트가 앱 본체라 `unsafe-inline`, Tailwind CDN 런타임
      컴파일 때문에 `unsafe-eval`이 불가피하다. 따라서 **이 CSP는 XSS 자체를 막지는 못한다.**
      실질적 방어선은 `connect-src`로, XSS가 발생해도 **훔친 데이터(특히 localStorage의 API 키)를
      외부 서버로 전송하지 못하게** 막는 것이다. XSS 차단은 여전히 `escapeHtml` 철저 적용에 의존한다.
    - **새 외부 API를 붙일 때는 반드시 `connect-src`에 추가할 것.** 빠뜨리면 그 기능만 조용히 죽는다
      (에러가 콘솔에만 뜨고 화면엔 "불러오지 못했습니다" 정도로만 보임).
    - 참고: 구글 로그인은 `signInWithOAuth` 전체 페이지 리다이렉트라 CSP 대상이 아니다.
      `form-action`은 OAuth 흐름을 깨뜨릴 여지가 있어 의도적으로 넣지 않았다.
    - **미검증 경로(로그인 계정이 없어 직접 못 돌림)**: 구글 프로필 사진 실제 표시,
      사용자 본인 AI 키로의 직접 호출(api.anthropic.com 등), Supabase WebSocket.
      도메인은 코드 grep으로 정확히 대조해 허용목록에 넣었고 `*.googleusercontent.com`까지 열어뒀지만,
      **로그인 후 실사용 시 AI 분석·프로필 사진이 정상인지 한 번 확인 필요.**

**뉴스 번역 배너 · AI 오류 메시지 개선 (완료)**

16. **[수정완료] 번역 실패가 조용히 영문으로만 나오던 문제.** MyMemory·Gemini 둘 다 무료 쿼터가
    Cloudflare 공유 IP 기준이라(이 앱만이 아니라 그 IP를 쓰는 전 세계 트래픽이 같이 소진시킴)
    자주 막히는데, 예전엔 Gemini가 429를 줄 때만 안내 배너가 떴다. `translateBatch`에서
    "번역을 시도했는데 결과가 원문 그대로면" 배너 플래그를 세우도록 수정 — 두 경로 다
    조용히 실패해도 이제 안내가 뜬다. `functions/news-free.js` + netlify 미러 동일 수정.
    **근본 해결(IP가 아니라 키 기준으로 세는 DeepL 등으로 교체)은 남은 과제** — 사용자가
    직접 키를 발급해야 해서 진행 전 상업적 사용 가능 여부부터 확인 필요.
17. **[수정완료] AI 분석 실패 시 본인 키 오류만 보여 오해 유발.** `callAI`는 본인 키 실패 →
    무료 AI 자동 대체 → 그것도 실패 시 원래(본인 키) 오류만 던졌는데, 이러면 "내 키만 고치면
    되겠네"로 오해하기 쉽다(실제론 무료 경로도 막힌 상태). 두 사유를 모두 보여주도록 수정.

**PC 데스크탑 반응형 (완료, 대시보드 우선순위 확인 후 전 탭 확장)**

18. **[완료] 데스크탑(1024px 이상) 전용 레이아웃 — 좌측 사이드바 구조로 재설계.**
    - **1차 시도(폐기)**: 대시보드만 폭을 넓히고 하단 탭바를 가운데 뜨는 독으로. 사용자 피드백:
      "여백이 많고 하단 독이 어색하다" → 폐기하고 아래 구조로 다시 짬.
    - **최종**: `header`/`bottom-nav`를 `position:fixed`로 뷰포트에 직접 고정. 사이드바(216px)는
      브라우저 왼쪽 끝, 헤더는 그 오른쪽부터 화면 끝까지 — Notion/Slack류 표준 골격.
      본문은 `margin-left`로 사이드바를 피하고 자체 `max-width`(1200px)로 가독폭 유지.
      **전 탭 공통 적용**(대시보드 한정 아님) — DOM은 그대로 두고 CSS만 바꿔서 모바일 마크업을 재사용.
    - **CSS Grid+`position:sticky` 조합은 버렸다**: 그리드 안에서 sticky를 쓰면 크롬이 정적 위치를
      잘못 계산해 사이드바가 헤더 위로 겹치는 버그를 실측으로 확인(`align-self:stretch`든 `start`든
      동일 재현). `position:fixed`로 바꿔 근본 회피 — sticky의 컨테이닝 블록 계산에 얽힐 일이 없다.
    - 1920px 등 초광폭 모니터에서 과도한 우측 여백이 남지 않는지 확인(`max-width:1200px`가 적당했음).
19. **[완료] 대시보드 → 포트폴리오 → 나머지 7개 탭(뉴스·포모체크·원칙·일지·설정·더보기·성장)
    순으로 타이포·여백 확대.** "폰트·데이터가 작게 느껴진다"는 피드백(모바일 기준으로 만든 화면을
    그대로 넓혀서 생긴 문제)에 대응.
    - **방법**: JS 템플릿(카드 HTML)은 모바일 그대로 두고, `#panel-xxx` 스코프 CSS로 Tailwind
      유틸리티 클래스(`text-xs`, `text-sm`, `text-[10px]`, `p-4`, `mb-3` 등)를 재정의해 크기만
      키운다. `#panel-xxx`(ID, 특이도 1) + 유틸리티 클래스(특이도 1) = 합쳐서 2라 Tailwind
      원본(특이도 1)을 셀렉터 힘만으로 이긴다 — 순서 의존이나 `!important` 없이 안전하게 오버라이드.
    - 탭마다 따로 안 쓰고 `:is(#panel-a, #panel-b, ...)`로 9개 탭을 한 규칙에 묶었다 — 모바일
      디자인이 애초에 탭 전체가 같은 크기 체계를 공유해서. 탭마다 다른 값(대시보드 히어로 숫자,
      포트폴리오 버튼 패딩 등)만 개별 줄로 뺐다.
    - **목록형 탭(포트폴리오 종목·투자원칙·투자일지·포모체크 기록)은 2열 그리드로 전환**
      (원래 세로로만 쌓이던 카드가 넓은 화면을 못 써서). 항목이 1개뿐이거나 빈 안내 카드만
      있을 땐 `:only-child`로 전체폭 유지 — 반쪽만 채워지는 어색함 방지.
    - **뉴스 피드는 의도적으로 그리드화 제외**: 배너·로딩 문구가 기사 카드와 같은 목록에 섞여 있어
      그리드로 바꾸면 그 문구들만 반쪽 폭으로 어색해진다. 타이포만 확대.
    - **설정·성장·더보기는 반복 리스트가 아니라 단일 카드 스택**이라 그리드 없이 타이포만 확대.
    - 검증: 매 단계 로컬 dev 서버 + DOM `getBoundingClientRect`/`getComputedStyle` 실측으로
      확인(이 세션에서 브라우저 스크린샷 캡처 도구가 리사이즈 후 축소된 프레임을 캡처하는
      버그가 있어서, 스크린샷보다 DOM 실측을 더 신뢰함 — 다음에도 스크린샷이 이상하면 DOM
      측정으로 교차검증할 것). 모바일은 전 탭 헤더 `20px`(원래 크기) 유지 확인.

**사파리 하단바 가림 · 튜토리얼 최신화 · 필수항목 표시 (완료)**

20. **[완료] 아이폰 사파리에서 앱 하단 탭바가 사파리 자체 하단 툴바에 가려짐.**
    `.bottom-nav`가 `position:sticky`였는데, 사파리가 주소창/탭 전환줄을 접었다 폈다 하며
    실제 보이는 뷰포트 높이가 바뀌는 동안 sticky의 위치 계산이 어긋나 nav가 화면 밖으로
    내려가는 게 실측으로 확인됨. **`position:fixed`로 전환**(사파리가 dvh+fixed 조합을 더
    안정적으로 처리) — app-shell의 480px 중앙정렬을 fixed 기준으로 직접 복제하고, `main`에
    `padding-bottom: calc(96px + env(safe-area-inset-bottom))` 추가해 콘텐츠 가림 방지.
    - **이 과정에서 회귀를 하나 발견**: 데스크탑(1024px↑) 사이드바 규칙이 `position/left/width`는
      재정의하지만 `transform`은 안 건드려서, 모바일용 `translateX(-50%)`가 그대로 상속돼
      **사이드바가 화면 밖(x:-108px)으로 밀려남**. `transform:none` 명시로 수정. 코드만 봐서는
      안 잡히고 DOM 실측(`getBoundingClientRect`)으로 잡은 버그 — **CSS 속성은 규칙 단위가
      아니라 속성 단위로 캐스케이드된다**는 걸 놓치기 쉬우니, 어떤 요소에 여러 브레이크포인트
      규칙이 겹칠 땐 매번 "이전 규칙이 설정한 속성 중 이번 규칙이 안 건드리는 게 있는가"를
      점검할 것.
21. **[완료] 온보딩 튜토리얼이 예전 버전 기준이라 최신 기능 반영 안 됨 + 특정 지점에서 막힘.**
    - **'성장' 탭이 아예 언급 안 됨** — 이 세션에서 나중에 추가된 기능이라 튜토리얼 작성 당시
      존재하지 않았음. 투자원칙 다음, 8번째(마지막) 스텝으로 추가.
    - **실제 버그 재현**: 튜토리얼은 강조된 진짜 버튼(예: "+오늘 기록")을 사용자가 직접 누를 수
      있게 설계돼 있는데("실제 화면을 하나씩 눌러가면서"), 눌러서 모달이 열린 채로 "다음" 스텝
      으로 넘어가면 **모달이 안 닫히고 다음 안내 카드와 겹쳐서 뜸** — 사용자가 보낸 스크린샷의
      "막힌다"는 지점이 정확히 이 상태였다(투자일지 작성 모달 + "더보기" 안내 카드가 동시에 떠
      있었음). `goToTutorialStep`이 부르는 두 진입점(`renderTutorialExplanation`,
      `beginTutorialNavPrompt`) 시작에 `closeModal()`을 추가해 스텝 전환마다 열린 모달을 정리.
      JS로 정확히 같은 시나리오(모달 열기 → 다음 스텝)를 재현해 수정 전/후 비교로 검증함.
    - **PC(사이드바 구조)에서 "아래 버튼을 누르세요" 안내가 실제로는 왼쪽에 있어 안 맞음** —
      `window.innerWidth >= 1024` 기준으로 "아래/👇" ↔ "왼쪽/👈" 문구를 분기. 스포트라이트
      좌표 자체는 `positionTutorialSpotlight`가 실제 렌더된 요소 위치를 그대로 읽어 이미
      반응형이라 손댈 필요 없었음 — 문구만 방향이 틀렸던 것.
22. **[완료] 폼 필수 항목에 빨간 별표(`<span class="text-bad">*</span>`) 표시.**
    각 폼의 실제 제출 검증 로직(toast 에러가 뜨는 조건)을 하나씩 확인해서 **진짜 막는 필드만**
    표시했다(그냥 "있으면 좋아 보여서"가 아니라): 포트폴리오 종목추가(종목명·수량·평균매입가·
    현재가 — 수량/평균매입가는 신규 등록시에만), 거래하기(수량·가격), 포모체크(종목명/종목코드·
    이유 — 매수예정수량은 선택), 현금 입출금(금액), 투자원칙(제목·내용), 투자일지(내용 —
    제목/사진/해시태그는 이미 "(선택)"이 붙어 있어 제외). select 요소는 항상 값이 있어 제외.

**서비스명 재논의 + 로그인 화면 서비스 소개 페이지 추가 (완료)**

23. **[논의, 결론: 유지] 서비스명을 "포모체크"로 바꿀지 재논의함.** "포모"가 요즘 뉴스·SNS에서
    핫한 키워드라 유혹이 있었지만, ①포모체크는 이미 앱 내 핵심 *기능* 이름이라 전체 서비스명으로
    쓰면 나머지 기능(원칙·일지·성장 등)이 부속품처럼 느껴지고 ②"포모"는 사용자의 불안(문제)을
    가리키는 말이라 이름으로 걸면 브랜드가 그 감정에 계속 고정되며 ③유행어는 식지만 "워워"는
    이미 몇 달치 브랜드 자산(마스코트 워니, 페르소나명, 카피)이 쌓인 고유 자산이라는 이유로
    **투자워워 유지, "포모"는 태그라인에서만 활용**하기로 결론. 확정 슬로건 목록과 이 결정의
    근거는 [[feedback_investwowo_copy_tone]] 메모리에 저장해 다음 세션에서도 카피 쓸 때
    참조하게 함(사용자가 "앞으로 서비스 문장 잡을 때 참조해줘"라고 명시적으로 요청).
24. **[완료] 로그인(웰컴) 화면에 "이 서비스가 뭔지 먼저 보기" 진입점 추가.** 베타 배포용으로
    만든 클로드 아티팩트 소개 페이지(공감 pain point → 해결 한 줄 → 기능 6개 → 페르소나 →
    CTA 구조)를 **앱 안에 상시 접근 가능한 페이지로 내재화**했다 — 아티팩트 링크는 임시
    공유용이라 서비스 자체의 상시 진입점이 될 수 없었음. `#guide-overlay`로 구현, 앱 기존
    컴포넌트(.card/.row/.chip, iconBadge 색상)를 그대로 재사용해 붕 뜨지 않게 함(아티팩트는
    Google Font 별도 사용했지만 이건 앱의 Pretendard 그대로).
    - **주의할 점**: `hideWelcomeOverlay()`를 그대로 재사용하면 안 됨 — 그 함수는
      `fomoguard_onboarded` 플래그를 세워서 "온보딩 완료"로 취급하는데, 소개만 보고 로그인
      안 한 사람에게 이 플래그가 세팅되면 안 됨. 별도의 가벼운 전환 함수(`showGuideOverlay`/
      `hideGuideOverlay`)로 분리해서 처리, 실제로 플래그가 안 세팅되는 것까지 확인함.
    - **직접 잡은 버그**: 정적 HTML 구간(`<script>` 밖)에 실수로 `${iconBadge(...)}` 같은 JS
      템플릿 문법을 그대로 써서 화면에 문자 그대로 찍히는 실수가 있었음 — 정적 HTML에서는
      템플릿 리터럴이 평가되지 않는다는 당연한 사실을 놓친 것. 정적 마크업으로 바꿔 수정.
      **다음에도 스크립트 밖 정적 HTML에 JS 함수 결과를 넣고 싶으면, 그 함수의 실제 출력을
      손으로 풀어 쓰거나(이번처럼) 페이지 로드 시 JS로 채워 넣을 것 — `${}`는 안 먹는다.**

**포모체크 할루시네이션 방지 (완료) · 기술적 분석(차트) 기능 문의 대응**

25. **[논의] 테스터가 원칙에 "장기 이평 역배열, 단기 이평 정배열, 5일선·20일선이 매물대를 뚫는
    타이밍" 같은 차트 기술적 조건을 넣고 판단을 요청함.** 코드 확인 결과 `runFactCheck()`가
    AI에게 넘기는 정보는 원칙 원문·포트폴리오 요약·최근 일지·입력한 이유·(있으면) 현재가
    단일 시점뿐 — **일별 OHLCV 히스토리·이동평균·거래량·매물대 데이터는 전혀 없음.** 이런
    조건을 물으면 AI가 실제로는 모르는 값을 그럴싸하게 지어낼 위험이 있어 **당장은 프롬프트로
    막고(아래 26번), 진짜 기술적 분석 기능 자체는 아직 안 만들었다** — 만들려면 일별
    시가·고가·저가·종가·거래량 히스토리를 주는 시세 API, 이동평균 계산 로직, 매물대(거래량
    밀집 구간) 탐지 알고리즘까지 새로 얹어야 하는 별도의 큰 기능(남은 과제로 아래에 추가).
26. **[완료] 포모체크·AI 데일리분석 프롬프트에 [정보 정확성 원칙] 추가.**
    - **핵심 규칙 3가지**: ①AI가 실제로 받은 데이터 밖의 것(차트·거래량·매물대·뉴스·재무제표)은
      지어내지 말고 "확인할 수 없다"고 명시 — 포모체크는 이때 원칙의 핵심 조건이 확인
      불가능하면 `verdict`를 자동으로 "주의"로 떨어뜨리게 지시함. ②주어진 데이터로 직접
      확인되는 사실은 확신 있게 평서문으로. ③시장 전망처럼 원래 불확실한 추정에는 완곡한
      표현("~로 보입니다") + 대략적 확신도(%)를 붙이되, **모든 문장에 습관적으로 붙이지
      말 것**(사용자가 명시적으로 요구한 균형 — 확실한 것까지 애매하게 만들면 안 됨).
    - 포모체크는 JSON `reasons`/`advice` 안에 확신도 %를 자연어로 섞어 넣는 방식(스키마·렌더링
      변경 없음). AI 데일리분석은 4~6문장 흐르는 서사글이라 숫자 확신도는 안 붙이고 완곡한
      어투로만 처리(구조화된 verdict 카드가 아니라서 "(확신도 40%)" 식 표기가 어색해 보임).
    - **검증 방법**: 실제 API 키 없이 `callAI` 함수 자체를 목업으로 가로채서(실제 네트워크
      호출 없이) 새로 추가한 규칙 문자열이 최종 systemPrompt에 정확히 포함되는지, 그리고
      테스터가 실제로 물었던 문구로 `runFactCheck()` 전체 파이프라인이 에러 없이 끝까지
      도는지(JSON 파싱, 기록 저장, 렌더링) 확인함 — API 키가 없어도 이렇게 프롬프트
      조립·파이프라인 무결성은 검증 가능하다는 걸 기억해둘 것.

**포모체크 전문화 로드맵 합의 + 1단계 완료(원칙별 체크리스트)**

27. **[논의, 로드맵 합의] 테스터 핵심 피드백: "대시보드·포트폴리오는 증권 앱에도 다 있다,
    결국 핵심은 포모체크인데 더 전문적이고 디테일하고 내가 정한 원칙 기반으로 정확히
    분석돼야 한다. 템플릿/조건식 검증도 있으면 좋겠다."** — 이 통찰(포모체크가 유일한
    차별화 지점)을 이후 리소스 배분 기준으로 삼기로 함. 3단계 로드맵 합의:
    - **1단계(완료, 26번 아래)**: 결과를 원칙별 체크리스트로 재구성
    - **2단계(예정)**: 원칙에 "직접 체크하는 조건" 추가 — 차트처럼 AI가 검증 못 하는 조건을
      사용자가 스스로 체크하고 AI는 그 체크 결과를 종합하는 구조. 25번의 "차트 데이터 없음"
      한계를 UX로 우회하는 방법이자 "템플릿/조건식" 요청의 핵심
    - **3단계(예정)**: 매도 검토·보유 검토 등 범위 확장
    - **관련 사이드 논의**: "포모체크"로 서비스명을 바꿀지 재논의했으나 유지로 결론
      (23번 참고). 5대 대가(버핏·린치·달리오·멍거·코스톨라니) 투자 철학을 사용자 원칙에
      반영하는 과정에서, **대가 템플릿 기능도 투자자 1명 선택 시 포인트 여러 개가 원칙 1개에
      뭉쳐 등록되는 동일한 문제**가 있다는 걸 발견함(남은 과제에 추가) — 지금 당장은 템플릿
      대신 철학을 녹인 개별 원칙으로 직접 등록하는 방식으로 우회함.
28. **[완료] 1단계: 포모체크 결과를 원칙별 체크리스트로 재구성.**
    - JSON 스키마 `reasons`(문장 3개짜리 뭉치) → `checks`(라벨/상태/근거 구조화 배열)로 변경.
      시스템 프롬프트가 `state.principles`를 순회해 등록된 원칙 제목을 프롬프트에 명시적으로
      나열하고, "매수하려는 이유" 모드에서는 그 제목 순서 그대로 **하나도 빠짐없이** checks에
      넣도록 지시(개수까지 명시해서 누락·창작 방지).
    - status는 `부합/위배/확인불가`(원칙 기반) 또는 `참고`(일반 질문 모드) 4종, 뱃지 색으로
      구분(`checkStatusStyle` 함수 신규). **확인불가가 하나라도 있으면 전체 verdict를 자동으로
      "주의"로** — 26번(할루시네이션 방지)과 자연스럽게 결합되어, 이제 "이 원칙은 왜 판단이
      안 되는지"가 원칙 단위로 명확히 보임.
    - **하위 호환**: 기존에 저장된 기록(`reasons` 스키마)은 `renderFactCheckResult`가 감지해서
      예전 방식(불릿 목록)으로 그대로 렌더링 — 과거 기록을 열어도 안 깨짐.
    - 검증: `callAI`를 목업으로 가로채 가상 원칙 4개로 실제 파이프라인 실행 → 프롬프트에 원칙
      제목 4개가 정확히 포함되는지, 렌더링된 체크리스트가 상태별 색상(위배=빨강/부합=초록/
      확인불가=노랑)으로 나오는지, 기존 `reasons` 기반 과거 기록도 정상 렌더링되는지 확인함.

29. **[수정완료] 1단계 배포 직후 실사용자가 "검증 실패 - Expected ',' or ']' after array
    element in JSON" 에러를 겪음.** 사용자는 "크레딧 다 썼나?"로 오해했으나 **API 키·크레딧과
    전혀 무관** — 원칙을 8개 등록한 상태였는데, checks 배열이 원칙 개수만큼 길어져야 함에도
    `maxTokens`가 1단계 이전 그대로 2500 고정이라 응답이 다 채워지기 전에 잘려 JSON이 깨진 것.
    - `factCheckMaxTokens = min(4000, 1600 + 원칙수*220)`로 동적 계산(8개 기준 3360)으로 근본 수정.
    - **재발 방지용 안전장치**: `callAIWithOwnKey`가 Claude(`stop_reason`)/Gemini(`finishReason`)/
      OpenAI·Grok(`finish_reason`)의 "길이 제한으로 잘림" 신호를 확인해 명확한 에러로 변환.
      `functions/ai-free.js` + netlify 미러(무료 AI 경로, 실제로 이 경로를 탄 것으로 보임)에도
      동일하게 추가.
    - **에러 메시지 정확성도 같이 고침**: 화면 하단 힌트가 예전엔 에러 종류와 무관하게 항상
      "API 키가 올바른지, 크레딧이 남아있는지 확인해주세요"로 고정돼 있어서, 사용자가 애먼
      API 키 설정을 의심하러 갔다. 이제 에러 원인이 "잘림"인지 "진짜 인증 문제"인지에 따라
      힌트 문구가 분기됨.
    - **교훈**: 응답 구조를 원칙 개수·항목 개수에 비례해 늘리는 기능(체크리스트, 목록형 결과
      등)을 추가할 때는 **maxTokens도 그 개수에 비례해서 같이 늘려야 한다** — 새 기능
      추가할 때마다 잊지 말고 점검할 것. 또한 에러 메시지에 "확인해주세요" 같은 힌트를 달
      때는 **모든 에러 케이스에 똑같이 적용되는 문구인지** 반드시 검토할 것(원인이 다르면
      안내도 달라야 함).
    - 검증: `callAI`를 목업으로 가로채 사용자가 실제로 겪은 것과 같은 종류의 깨진 JSON(배열
      안 닫힌 채 객체만 닫힌 경우)을 재현해 새 에러 메시지·힌트가 정확히 뜨는지, 정상 응답
      (8개 체크 전부)도 문제없이 렌더링되는지 확인함.

**남은 과제 (우선순위 순)**
- 포모체크 2단계: 원칙에 "직접 체크하는 조건(템플릿/조건식)" 추가하는 기능 (27번 참고)
- 대가 템플릿(`openTemplatePicker`)도 투자자 1명당 포인트 여러 개가 원칙 하나에 뭉쳐서
  등록되는 문제가 있음 — 나중에 포인트 단위로 개별 원칙 등록하도록 개선 검토 (27번에서 발견)
- 진짜 차트 기술적 분석 기능(이평선 배열·매물대 등) — OHLCV 히스토리 API 조사부터 필요,
  현재는 프롬프트로 "지어내지 않기"만 막아둔 상태(25·26번 참고)
- API 키 보관 구조 근본 개선(서버 보관 또는 동기화 제외)
- 뉴스 번역: IP 공유 쿼터 문제의 근본 해결(DeepL 등 키 기준 API로 교체) — 사용자 판단 필요
- 대시보드 정보 밀도: 8섹션 2.8화면 (편집 진입점은 12번에서 해결. 기본 노출 섹션을 줄일지는
  실사용 데이터를 보고 판단하는 게 나음 — 임의로 숨기면 기능 발견이 어려워짐)
- 코어/알파 용어: 설명 아이콘은 있으나 첫 사용 시 한 줄 설명 노출 검토
- PC 데스크탑: 설정 탭 폼 레이아웃(2열 배치 등 좀 더 다듬을 여지), 로그인 후 실사용 시
  AI 분석·구글 프로필 사진이 CSP 아래서 정상인지 실사용자 확인 필요(18번 CSP 항목 참고)

### 세션 4 — 2026-08-24, 세션 3과 같은 컴퓨터

1. **브랜드 컬러/로고/캐릭터명 1차 컨셉 논의** (코드 미반영, 논의만): 브랜드 컬러는 토스블루(#3182f6)와 겹치지 않으면서도 그 임팩트에 근접한 **#1E8FFF(아이스블루)**로, 로고는 앱 안에서 이미 "냉철함"을 상징하던 🧊를 모티브로 한 **각얼음 캐릭터 로고**(굵은 외곽선 + 둥근 형태로 캐릭터 일러스트 스타일과 통일감), 캐릭터명은 **"워니"**로 논의 완료. 사용자가 이 내용을 다른 AI에게 전달해 최종 로고 아트를 뽑아올 예정 — **받으면 코드(브랜드 컬러 변수, 파비콘, 앱아이콘, 헤더 워드마크)에 적용 필요**.
2. **세션 3에서 다른 컴퓨터가 푸시한 커밋 학습·검증**: `git pull`로 반영된 세션 3의 마지막 두 커밋(HANDOFF.md 갱신 + 트리맵 누적/오늘변화 토글·섹션 설명 아이콘·포트폴리오 인라인 전일대비·매수매도 색상 수정 커밋)을 diff로 정확히 파악하고, 실제 배포 서버에서 트리맵 토글 전환·포트폴리오 인라인 표시가 정상 작동하는 것까지 직접 확인함.
3. `NEWSDATA_SHARED_KEY`/`CURRENTS_SHARED_KEY` Cloudflare 환경변수 등록 완료 및 실제 배포 서버에서 정상 작동 확인(위 3번 섹션 갱신 반영).
4. **`supabase-growth.sql` 실행 여부 확인**: 대시보드 로그인 없이도, `SUPABASE_ANON_KEY`로 growth RPC(예: `grant_login_xp`)를 인증 없이 직접 호출해보면 알 수 있음 — 테이블/함수가 없으면 "Could not find the function" 류의 404가 오고, 있으면 `user_id` NOT NULL 위반 같은 정상적인 DB 에러가 옴(비로그인 호출이라 막히는 게 정상이므로 이 에러가 곧 "실행됨"의 증거). 실제로 호출해서 후자를 확인 → 실행 완료로 판정함.
5. **사용자 리포트 5건 진단·수정**:
   - 로그인 세션이 로그아웃 전까지 유지되는 건 Supabase 세션 영속성 표준 동작(정상) — 코드 변경 없음.
   - "총 손익금액"에 실현손익(매도 익절/손절)이 이미 포함돼 있음을 `computeTotalPnl()`로 확인 — 변경 없음.
   - AI 데일리 분석 "로그인 확인에 실패했습니다" 오류: `increment_ai_usage` RPC 자체는 정상 배포돼 있음을 서버에 직접 확인했지만, `/ai-free`가 캡 초과 외의 모든 RPC 실패를 이 문구 하나로 뭉뚱그려서 진짜 원인을 알 수 없었음 → Supabase가 반환한 실제 오류 텍스트를 그대로 노출하도록 수정(다음에 재현되면 진짜 원인이 바로 보임).
   - 무료 뉴스 "최근 3일 내 뉴스 없음": 1차로 `NEWS_CACHE_VERSION`을 2→3으로 올려 예전 빈 캐시를 무효화했지만, 사용자가 반도체/삼성전자/나스닥/코스피 전부 여전히 0건이라고 재확인 → 근본 원인은 따로 있었음. `news-free.js`가 한국어 검색어를 영문으로 바꿀 때 쓰던 **구글 번역 공개 엔드포인트(translate.googleapis.com)가 클라우드플레어발 요청을 429("Sorry...")로 하드 차단**하고 있었음(실제 raw 응답을 서버 로그로 찍어서 확증). 정식 키가 없는 공개 스크래핑 엔드포인트라 언제든 이런 식으로 막힐 수 있는 구조였음. → 이미 안정적으로 쓰던 정식 Gemini API(`GEMINI_SHARED_KEY`)로 번역을 완전히 교체(`translateBatchWithGemini`, 여러 문자열을 번호 매긴 리스트로 한 번에 배치 번역). 반도체/삼성전자/나스닥/코스피 4개 키워드 전부 실제 배포 서버에 curl로 재현 테스트해서 각 10건씩 정상 번역된 실제 기사가 나오는 것 확인 완료(2026-08-24). 단, 기사 수를 15→10건(안내 문구 "최대 10건"에 맞춤)으로 줄여도 응답 시간이 키워드당 약 18~22초 소요됨(첫 배치 번역 시도 시 38초 걸린 것에서 개선) — Gemini 배치 번역 호출 자체가 느린 편이라, 클라이언트가 여러 키워드를 순차로 새로고침하는 `fetchAllNewsKeywords()` 특성상 키워드 여러 개 등록 시 전체 새로고침에 다소 시간이 걸릴 수 있음(키워드별로는 화면에 순차적으로 채워짐, 하루 1회만 발생 - 이후엔 캐시). 추가 최적화 여지 있음(예: 배치 크기 축소, 프롬프트 단순화, 더 빠른 모델 사용 등) — 사용자가 실사용해보고 느리다고 느끼면 재검토 필요.
   - **[후속, 같은 날] 사용자가 재확인 후 재보고**: "나스닥/삼성전자는 영문 기사로 나오고, 코스피는 여전히 뉴스 없음". 재진단한 결과 두 가지 문제가 겹쳐 있었음:
     1. **스테일 캐시**: `NEWS_CACHE_VERSION`이 안 올라간 상태에서 이미 오늘자로 캐시된 결과(비어있거나 미번역)가 "오늘 캐시라 최신"으로 취급돼 재조회가 막힘 → 버전 3→4로 재차 올려 무효화. `fetchNewsForKeywordFree`가 서버가 내려준 `translated` 플래그를 무시하고 항상 `false`로 하드코딩해서 "🌐 번역" 배지가 무료 유저에게 절대 안 뜨던 버그도 같이 수정.
     2. **[진짜 근본 원인] `GEMINI_SHARED_KEY` 무료 티어 하루 요청 한도(20건) 소진**: 임시 debug 모드를 추가해 NewsData/Currents/Gemini 각각의 원시 HTTP 응답을 직접 캡처해서 확증. 코스피 검색 시 NewsData·Currents는 둘 다 200 OK로 정상 응답했지만 검색어 자체가 번역 안 된 원문 한글("코스피")인 채로 영어전용(`language=en`) API에 들어가서 0건이 나온 것이었고, 그 원인은 Gemini 호출이 `429 Quota exceeded ... limit: 20`으로 막혀있었기 때문. `/ai-free`(AI 데일리 분석)와 `/news-free`의 키워드번역·기사번역 호출이 전부 같은 `GEMINI_SHARED_KEY`/같은 모델(`gemini-3.6-flash`)을 공유해서, 가벼운 테스트만으로도 하루 한도가 바로 소진됨. 나스닥/삼성전자가 영문으로 나온 것도 같은 이유(검색어 번역엔 성공했지만 그 다음 기사 본문 번역 Gemini 호출에서 쿼터 소진으로 막혀 원문 영어가 그대로 노출됨) — 동일 원인이 두 가지 다른 증상(빈 결과 vs 미번역)으로 나타난 것.
     - **적용한 완화책(코드만으로 가능한 부분)**: `news-free.js`에 자주 쓰이는 한국어 금융 키워드(코스피/나스닥/삼성전자/반도체/비트코인 등 30여개) → 영문 사전(`KEYWORD_DICTIONARY`)을 추가해서, 사전에 있는 키워드는 Gemini 호출 없이 즉시 영문 검색어로 매핑되게 함. 이러면 "검색어 번역 실패로 인한 0건"은 사전에 있는 키워드에 한해 근본적으로 재발 안 함(Gemini 쿼터 상태와 무관). 실제 배포 서버에 코스피로 재현 테스트해서 검색은 정상 10건 나오는 것 확인함(2026-08-24) — 단, **기사 본문 한→영 번역은 여전히 Gemini가 필요**해서, 그날 쿼터가 이미 소진된 상태면 기사 텍스트 자체는 당분간 영어로 보일 수 있음(쿼터는 통상 UTC 기준 자정 리셋으로 추정, 정확한 리셋 주기는 미확인).
     - **완전히 해결하려면 사용자 판단이 필요한 부분**: 사전에 없는 키워드(사람 이름, 신조어 등)의 검색어 번역과, 모든 키워드의 기사 본문 번역은 구조적으로 Gemini에 의존함. 무료 티어 20건/일로는 실사용(여러 유저 × AI 분석 3회/일 × 뉴스 키워드 여러 개)을 감당 불가.
   - **[같은 날, 5차] 번역이 여전히 잘 안 된다는 재보고 → MyMemory Translation API(키 불필요) 도입 시도했으나 근본 해결 실패**: Gemini 무료 쿼터가 그날 계속 소진 상태였던 걸 확인하고, 완전히 분리된 무료 리소스로 번역을 옮기기 위해 MyMemory Translation API(`api.mymemory.translated.net`, 키 불필요, 공식 익명 한도 5,000단어/일)를 1차 번역 수단으로 도입하고 Gemini는 실패시 보조로만 쓰도록 재작성함(`translateBatch` 함수, `news-free.js`+`news-proxy.js` 양쪽 다). 배포 후 재확인했더니 **MyMemory도 이미 그날 쿼터 소진 상태(429, "YOU USED ALL AVAILABLE FREE TRANSLATIONS FOR TODAY")**였음 — 임시 debug 계측을 심어서 원시 응답으로 확증. 원인은 Cloudflare Pages Functions가 나가는 IP가 **Cloudflare 전체 고객사가 공유하는 대역**이라, MyMemory의 익명(IP 기준) 일일 한도가 이 앱 하나의 사용량이 아니라 그 IP 대역을 쓰는 전 세계 수많은 다른 서비스의 트래픽까지 합쳐서 소진되는 구조적 문제였음. MyMemory 공식 문서상 `de=이메일` 파라미터를 붙이면 더 높은 한도(50,000단어/일)가 적용된다고 되어 있어 실제로 시도(`de=noreply@investwowo.app`)했지만 배포 서버에서 재현 테스트해보니 **동일하게 429**가 나서 효과가 없었음(이메일 기준이 아니라 여전히 IP 기준으로 걸리는 것으로 추정) — 즉 Google Translate 공개 엔드포인트 때와 똑같은 "클라우드 공유 IP발 요청은 무료 익명 번역 서비스에서 구조적으로 불리하다"는 패턴이 다른 서비스에서도 반복 확인됨.
     - **현재 상태**: MyMemory(1차)+Gemini(2차 보조) 폴백 체인 자체는 유지함 — MyMemory가 IP 혼잡이 덜한 시간대엔 정상 작동할 수도 있고, 코드 구조상 손해볼 게 없어서 그대로 둠. 다만 두 리소스 다 IP/쿼터 공유 문제가 있어서 **번역이 언제든 일시적으로 실패할 수 있다는 전제**는 여전히 유효함 — 그 경우 이미 구현된 `translationLimited` 배너로 사용자에게 투명하게 안내됨. 검색(뉴스가 아예 안 나오는 문제)은 키워드 사전 덕분에 이 문제와 무관하게 항상 정상 작동함.
     - **진짜 근본 해결책(사용자 결정 필요)**: 요청량이 IP가 아니라 **발급받은 키 단위로 추적되는** 번역 API로 바꿔야 클라우드 공유 IP 문제에서 근본적으로 자유로워짐. 후보: DeepL API Free(월 500,000자 무료, 가입 필요) 등. 이건 NEWSDATA_SHARED_KEY/CURRENTS_SHARED_KEY 때처럼 사용자가 직접 가입해서 키를 받아 Cloudflare 환경변수에 등록해야 하는 사안이라 임의로 진행하지 않고 여기 남겨둠 — 사용자가 원하면 다음 세션에서 진행.
   - **[같은 날, 3차] 서비스 철학 명확화**: 사용자가 "개인 Pro 계정을 공동 인프라로 쓰려는 게 아니다"라고 정정 — 유료 티어 전환 제안은 철회. 대신 사용자가 처음부터 원했던 방향(무료 우선, 한도 초과 시 본인 키 등록 유도 안내)을 서버 응답 레벨에서 명시적으로 구현:
     - `/ai-free`: Gemini 자체가 429(공유 쿼터 소진)를 반환하면 기존의 뭉뚱그린 "AI 서버 오류" 대신, 개인별 하루 3회 한도 초과 메시지와 같은 톤으로 "오늘의 무료 AI 사용량이 모두 소진됐어요(전체 이용자 공통 한도). 설정에서 본인 API 키를 등록하면 제한 없이 계속 이용할 수 있어요"를 반환하도록 수정.
     - `/news-free`, `/news-proxy` 둘 다: `translateBatchWithGemini`가 429를 만나면 `quotaFlag.hit=true`로 표시하고, 최종 응답에 `translationLimited` 필드를 포함시킴. 클라이언트(`renderNewsFeed`)가 이 값을 읽어 뉴스 목록을 막지 않으면서(기사는 그대로 보여줌) 인라인 배너로 안내 — GNews 키 미등록 유저는 "본인 뉴스 API 키를 등록하면 언제나 자동 번역까지" CTA를, 이미 키를 등록한 유저는 "일시적 혼잡" 문구로 다르게 안내(이미 키가 있는데 또 등록하라고 하면 혼란스러우니까).
     - **`/news-proxy`(본인 GNews 키 등록 경로) 번역 로직 자체도 같이 교체**: 기존엔 기사마다 개별적으로(최대 20회) 구글 번역 공개 엔드포인트를 호출하고 있었음 — 이건 오늘 이미 free 경로에서 막힌 걸로 확인된 바로 그 취약한 엔드포인트라, "본인 키 등록하면 번역까지 확실히 된다"는 안내가 거짓이 될 뻔했음. `news-free.js`와 동일한 `translateBatchWithGemini`(배치 1회 호출) + 키워드 사전(`KEYWORD_DICTIONARY`)으로 교체해서 신뢰성과 호출 효율 둘 다 개선. 배포 서버에 `news-proxy` 빈 요청으로 문법 오류 없이 정상 배포된 것까지 확인(2026-08-24). 실제 GNews 키로 종단 테스트는 아직 안 함(테스트용 키 없음) — 사용자가 본인 키로 실사용해보고 이상 있으면 재확인 필요.
   - **[같은 날, 4차] 유료 API 등록한 본인 계정에서도 오류 재현 — 3가지 원인 발견/수정**:
     1. **[내가 만든 버그] `news-proxy.js`에 번역 성공 여부와 무관하게 `a.translated=true`를 무조건 세팅하는 실수**: `news-free.js`는 실제로 번역된 경우에만 `translated=true`를 세팅하는데, `news-proxy.js` 재작성 때 이 조건을 빠뜨려서 Gemini 번역이 (공유 쿼터 소진으로) 실패해도 "🌐 번역" 배지가 뜨면서 영문 원문이 그대로 노출됨 — 사용자 스크린샷("Stock Market Outlook..." 등 영문 기사에 번역 배지)으로 확인. `news-free.js`와 동일한 조건부 세팅으로 수정.
     2. **GNews 429 "made too many requests in a short period"**: 일일 한도가 아니라 순간 버스트 제한. `searchGNews` 재시도를 1회→2회, 대기시간도 고정 1.5초→지수적 1.8초/3.2초로 강화. 클라이언트 `fetchAllNewsKeywords()`의 키워드간 대기도 400ms→900ms로 늘림.
     3. **AI 데일리 분석 "Gemini API 오류 (429): You exceeded your current quota..."**: 이건 우리 공유 키가 아니라 **사용자 본인이 설정에 등록한 개인 Gemini 키가 직접 Google로부터 받은 429**임(클라이언트가 본인 키 등록 시 `/ai-free`를 거치지 않고 브라우저에서 Google API를 직접 호출 — `callAI()`의 `provider==='gemini'` 분기, index.html:350). 서버 코드로는 고칠 수 없는 사용자 계정/결제 문제. 에러 메시지에 힌트를 추가(`ownKeyApiError()`)해서 "본인이 등록한 API 키의 요청 한도(또는 결제 설정)를 초과했어요 — 해당 콘솔에서 쿼터·결제 확인, 다른 모델도 시도해보세요"가 뜨게 함(Claude/OpenAI/Grok도 동일 패턴 적용). **사용자에게 안내한 추정 원인**: (a) 키를 발급받은 Google Cloud 프로젝트와 결제가 활성화된 프로젝트가 다를 가능성, (b) 기본 모델 `gemini-3.6-flash`가 프리뷰 단계라 결제 여부와 무관하게 별도의 낮은 한도가 있을 가능성 — 설정에서 `gemini-2.5-flash`(더 안정화된 모델)로 바꿔보는 것을 권장.
   - "데이터 초기화"는 회원탈퇴가 아님(로컬+클라우드 데이터만 삭제, 계정은 유지) 확인. 기존엔 로그아웃을 안 시켜서 세션이 남아있으면 포커스 복귀 시 클라우드 데이터가 도로 채워지는 버그가 있어 수정(초기화 시 빈 상태를 클라우드에 push 후 로그아웃까지 시킴). **신규로 진짜 회원탈퇴 기능 추가**: `/delete-account` — `SUPABASE_SERVICE_ROLE_KEY`로 `fomoguard_data`/`fomoguard_growth`/`fomoguard_ai_usage` 전 테이블 데이터 + Supabase Auth 계정 자체를 서버에서 영구 삭제. 클라이언트는 본인의 로그인 토큰으로만 호출 가능(서버가 토큰으로 실제 사용자를 재확인, 클라이언트가 보낸 id는 신뢰 안 함). **`SUPABASE_SERVICE_ROLE_KEY` 미등록 상태라 아직 실제로 작동 안 함** — 위 2번 섹션 참고해서 등록 필요.

### 세션 3 — 2026-08-21~22, 다른 컴퓨터

1. **튜토리얼을 스포트라이트/코치마크 방식으로 전면 재작성**: 정적 슬라이드 대신, 실제 화면 위에 어둡게 처리 + `clip-path`로 뚫린 구멍(스포트라이트)을 씌워 진짜 버튼을 비추고, 탭이 다른 화면에 있는 단계는 "다음" 버튼 대신 **실제 버튼을 직접 클릭해야** 다음 단계로 진행되는 클릭스루 방식(`switchTab()`에 훅). 더보기 오버플로우에 있는 탭은 더보기 버튼 → 실제 메뉴 행 순서로 2단계 스포트라이트. 노출 조건은 `state.settings`(클라우드 동기화)에 기록: 최초 로그인 후 24시간 이내에만 노출, "다시 보지 않기"나 투어 완주("모두 이해했어요")는 영구 dismiss, 그냥 건너뛰기는 24시간 내 재노출 가능.
2. **RPG식 성장 시스템 신설 (🌱 성장 탭)**: 레벨/XP 진행바, 레벨업 커브는 뒤로 갈수록 가팔라짐(`xpForLevel(n) = 40*n^1.4+20`, 매일 꾸준히 하면 Lv.21까지 약 6개월). 칭호는 레벨(누적 XP)과 트랙(포트폴리오의 원화/달러 비중으로 실시간 계산되는 동학개미/서학개미/밸런스) 두 축으로 결정 — 같은 레벨이라도 포트폴리오 구성에 따라 칭호가 다름(주린이 → 일개미 → 동학개미/서학개미/동서학개미 → 여의도/월가/재미교포 개미 → 존리/워런/올웨더 개미 → 여의도·월가·올웨더 전설의 여왕개미). 레벨·트랙별 개미 캐릭터 이미지 18종(`mascot/rank/t1~t6-*.png`)을 헤더 배지·성장 탭에 표시. 매일 바뀌는 투자 상식 퀴즈(24문항 뱅크, 날짜 인덱스로 1일 1문제, 정답 시 +25 XP). XP 출처: 첫 로그인/일(+10), 포모체크(+15, 1일 3회 캡), 투자일지 작성(+15, 1일 3회 캡, 신규 작성만), 퀴즈 정답(+25, 1일 1회).
3. **XP 서버 검증 전환** (위 3번 섹션 참고) — `supabase-growth.sql` 실행 필요.
4. **대시보드 섹션 편집 모드**: 카드를 2초 길게 누르면 흔들림(jiggle) 모드 진입, 오른쪽 손잡이(☰)만 드래그해서 순서 변경(카드 본문은 편집 모드에서도 평소처럼 스크롤 가능하도록 손잡이에만 `touch-action` 제한), 🗑 배지로 숨기기 → 하단 "보관함" 칩에서 복구. iOS Safari 길게 누르기 시 텍스트 선택 콜아웃이 먼저 뜨는 문제는 `-webkit-touch-callout:none; user-select:none`으로 해결.
5. **포모체크 입력 개선**: "투자 예정 금액" 대신 **"매수 예정 수량"**을 입력받고, 실시간 시세를 조회해 수량×현재가로 환산 금액을 계산 → 그 금액과 현재가를 AI 검증 프롬프트에 포함. Twelve Data 키가 없어도 `/price-free`(Yahoo Finance 기반, 키 불필요) 경로로 자동 대체 — 대시보드의 코스피·S&P500·나스닥 지수도 동일 경로로 키 없이 조회 가능해짐.
6. **뉴스 무료 기본 제공**: `/news-free`(NewsData.io 메인 + Currents API 보조 조합, 둘 다 무료 티어 상업적 사용 허용)를 추가해 GNews 키가 없어도 뉴스가 기본 동작. GNews 키를 이미 등록한 사용자는 기존 `/news-proxy`를 그대로 사용(동작 변경 없음). (참고: 처음엔 구글 뉴스 RSS로 시도했으나 클라우드 IP발 요청을 간헐적으로 503 차단하는 걸 실측 확인 → 정식 등록 키 기반의 NewsData/Currents 조합으로 교체. Guardian Open Platform은 무료 키가 비상업 전용이라 라이선스 리스크로 제외.)
7. **AI 데일리 분석 근거 확충**: 시장지표 몇 개뿐이던 프롬프트 근거를 보유종목명+티커+수익률+전일등락률, 투자원칙 전문, 관심 키워드의 오늘자 실제 뉴스 헤드라인까지 확장. 매일 다른 표현을 쓰도록 조정(단, 제공된 뉴스 헤드라인에 없는 내용은 여전히 금지).
8. **포모체크 기록 삭제**: 최근 기록 행에 확인 다이얼로그가 있는 삭제(✕) 버튼 추가(기존 클릭-재열람 기능과 별도).
9. **크로스 디바이스 새로고침**: 로그인 상태에서 탭이 다시 포커스/보이게 될 때 클라우드를 재-pull(3번 섹션 참고).
10. **도넛 차트 툴팁 줄바꿈 수정**, 대시보드 소수점 반올림 표시.

### 세션 2 — 2026-08-21~22, 이 컴퓨터 (클론 직후)

1. **탭 순서 편집을 드래그 방식으로 교체**: 화살표 버튼 대신 오른쪽 손잡이(☰)를 Pointer Events로 눌러 끄는 방식(마우스/터치 공용)으로 즉시 원하는 위치에 이동.
2. **현금 출금 기능 추가**: 기존엔 입금만 가능했음 → 입금/출금 세그먼트 토글, 잔고 초과 출금 방지, 출금도 투자일지 거래내역 타임라인에 매수/매도와 함께 노출.
3. **비로그인 "둘러보기" 예시 데이터 자동 채움**: 삼성전자·SK하이닉스(코어) + 테슬라·엔비디아(알파) 4종목, 매수/매도/입출금 내역, 투자원칙 3개, 일지 2개, 포모체크 예시 1건, 7일치 자산 추이를 한번에 시딩해서 모든 탭을 바로 체험 가능하게 함. **실제 로그인 시 이 데모 데이터는 자동으로 지워지고 계정 데이터로 전환**(안 그러면 새 클라우드 계정에 데모 데이터가 그대로 push될 위험이 있었음).
4. **더보기 탭에 로그인 유도 CTA**: 비로그인 둘러보기 중엔 더보기 탭 상단에 "지금은 둘러보기 중이에요" + Google 로그인 버튼 노출.
5. **전일대비 일간 변화량 표시**: 대시보드 총자산에 누적 수익률과 별도로 스냅샷 기반 "전일대비 +N%" 배지, 포트폴리오 종목별로도 TwelveData 시세의 `percent_change` 기반 개별 등락 표시.
6. **첫 진입 튜토리얼(정적 슬라이드 버전)** — ⚠️ 세션 3에서 스포트라이트 방식으로 전면 재작성되어 현재는 대체됨. 아래 "알아두면 좋은 함정"의 관련 항목도 세션 3 기준으로 갱신됨.

### 세션 1 — 2026-08-20

1. **Netlify → Cloudflare Pages 이관**: 크레딧 소진 대응. GitHub 연동으로 자동 배포 세팅, Supabase Redirect URL에 새 도메인 추가 필요했음(사용자가 직접 처리).
2. **현금 입력 USD/KRW 통화 선택 + 실시간 환율 환산 저장**, **투자 격언 113→184개로 확대**, **날짜 고정 로테이션 → 새로고침마다 랜덤**.
3. **뉴스 기능 대수술**: GNews 무료 플랜이 배포 도메인에서 CORS로 직접 호출을 막는 걸 발견 → `/news-proxy` 서버 프록시로 우회. 한글 키워드는 서버에서 영문 번역 후 국내/해외 이중 검색·병합·중복제거, 비한국어 기사는 Google Translate 공개 엔드포인트로 한국어 번역. 이중 검색으로 인한 GNews 429는 순차 실행+재시도로 해결. 전체 모아보기/키워드별 보기, 호재·악재 자동 배지, 캐시 스키마 버전 도입.
4. **API 키/설정도 Supabase에 클라우드 동기화**.
5. **마스코트 캐릭터(개미) 도입**: 표정 9종(face-A~I) + 포즈 7종 + 앱 아이콘. 대시보드 총자산 카드의 마스코트는 사용자 요청으로 삭제(재추가 금지). 엄지척/활짝 웃는 표정은 세션 1 시점엔 미확보였으나 이후 세션에서도 언급 없음 — 여전히 미확보 상태로 보임.
6. **서비스명 전체 리브랜딩**: 포모가드 → 투자워워(가칭).
7. **대시보드 개선**: "총 손익금액" 항목 추가, 코어/알파 수익률 같은 줄 배치.
8. **명대사 패러디 14개** 추가(패러디, 원본 그대로 인용 아님).
9. **포모체크 기록 다시보기** 버그 수정(클릭 안 되던 정적 목록 → 재열람 가능).
10. **코어/위성 → 코어/알파 명칭 변경** + 로컬 키워드 매칭 기반 자동 추천.

## 5. 알아두면 좋은 함정들

- **캐시 무효화**: `_headers` 파일에서 `/`, `/index.html`은 `Cache-Control: no-cache`. 그런데도 iOS Safari가 예전 버전을 계속 보여주는 경우가 있었음 → 사용자에게 Safari 사이트 데이터 삭제를 안내해서 해결. 근본 원인은 명확히 못 찾음, 재발 시 참고.
- **iOS 길게 누르기 충돌**: 길게 누르는 UI(대시보드 카드 편집 등)를 iOS Safari에서 쓸 땐 텍스트 선택 콜아웃이 먼저 뜨는 걸 `-webkit-touch-callout:none; user-select:none`으로 막아야 함.
- **GNews 무료 플랜**: 배포 도메인 직접 CORS 호출 불가, 순간 요청 제한 있음 → 서버 프록시 + 순차 요청 유지. 키가 없는 사용자는 `/news-free`(NewsData/Currents)로 자동 대체됨.
- **구글 번역 공개 엔드포인트(`translate.googleapis.com`)**: 짧은 시간 연속 호출 시 간헐적으로 429 반환 — 재시도 로직 필요(뉴스 번역, `/news-free`에 이미 반영됨).
- **Yahoo Finance 기반 무료 시세(`/price-free`)**: 키 불필요, TwelveData 키가 없거나 실패했을 때 자동 폴백으로 사용.
- **로컬 개발 서버**: `.claude/launch.json`에 `fomoguard` 설정 있음(`npx serve fomoguard -l 5544`, 이 컴퓨터는 Node를 새로 설치해서 PATH 문제로 `run-fomoguard.cmd` 래퍼를 씀 — 새 터미널을 열면 PATH가 정상 반영되어 원래 커맨드로도 됨). `/ai-free`, `/news-proxy`, `/news-free`, `/price-free` 같은 Functions는 로컬 static 서버에서는 404남(정상, Cloudflare에만 존재) — 실제 동작 확인은 배포 후 검증.
- **XP 조작 방지**: 성장(레벨/XP)은 클라이언트 계산을 신뢰하지 않음 — 반드시 Supabase RPC(security-definer)를 거쳐야 함(`supabase-growth.sql` 실행 완료 확인됨). 비로그인 게스트는 예전처럼 로컬 전용 폴백 유지.
- **이미지 처리**: 프로젝트 루트 `node_modules`에 `sharp` 설치되어 있어서 마스코트 자산 가공에 사용함. sharp의 raw buffer 체이닝에서 `extract().trim()`을 한 파이프라인에 묶으면 에러 나는 경우가 있었음 → 두 단계로 분리(encode 후 재로드)하면 해결됨.
- **JS 코드 문법 검사**: `index.html`의 `<script>` 블록만 추출해서 `node --check`로 검증하는 방식을 계속 사용함(별도 빌드 도구 없음).
- **`functions/`와 `netlify/functions/`는 항상 쌍으로 수정**: 두 폴더에 거의 동일한 로직이 미러링되어 있음(Netlify는 롤백 대비용이라 실사용은 안 하지만, 로직이 갈라지면 나중에 헷갈림).

## 6. 다음에 하면 좋을 것들 (미결 아이디어/작업, 확정 아님)

- ~~`supabase-growth.sql` 실행~~ — 2026-08-24 실행 완료 확인함(익명 키로 RPC 직접 호출해서 테이블·함수 존재 검증).
- ~~Cloudflare에 `NEWSDATA_SHARED_KEY`, `CURRENTS_SHARED_KEY` 환경변수 등록~~ — 2026-08-24 완료, 배포 서버에서 정상 작동 확인함.
- **⚠️ 우선순위 높음 — Cloudflare에 `SUPABASE_SERVICE_ROLE_KEY` 환경변수 등록**: 회원탈퇴(`/delete-account`) 기능이 이 키가 있어야 실제로 계정을 삭제할 수 있음. Supabase 대시보드 → Settings → API → "service_role secret". 없으면 회원탈퇴 버튼을 눌러도 에러만 반환하고 앱은 안전함(계정 삭제 안 됨).
- 브랜드 컬러(#1E8FFF 아이스블루)·로고(각얼음 모티브)·캐릭터명("워니") 1차 컨셉 논의는 끝났지만 **아직 실제 코드에 적용 안 됨** — 사용자가 다른 AI로 최종 로고 아트를 뽑아온 뒤 적용하기로 함. 지금 코드는 여전히 기존 브랜드 컬러(#3182f6, 토스블루와 동일) 그대로임.
- 서비스명 최종 확정 → 전체 치환 + 도메인/GitHub repo 이름도 바꿀지 결정
- 엄지척 표정 마스코트 받으면 실적 달성 등 긍정 상황에 배치 (여전히 미확보)
- 파비콘/PWA 아이콘이 `icon-192.png` 하나뿐 — apple-touch-icon 별도 사이즈, manifest.json(PWA 설치 지원) 아직 없음
- 커스텀 도메인 연결(현재 `pages.dev` 서브도메인만 사용 중)

## 7. 새 세션 시작 명령어 참고

```bash
git clone https://github.com/FOMOGUARD/fomoguard.git
cd fomoguard
git log --oneline -20   # 최근 작업 확인
```
그 다음 Claude Code에게 "이 HANDOFF.md 읽고 투자워워 프로젝트 이어서 진행할게" 라고 말하면 됨. **다른 컴퓨터에서 동시에 작업 중일 수 있으니, 시작 전에 항상 `git pull`로 최신 커밋을 받았는지 먼저 확인할 것.**
